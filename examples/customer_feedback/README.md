# Customer Feedback Example

Short support tickets labeled for urgency and sentiment.

- `sample.csv` columns: `ticket`, `urgency` (`High`/`Low`), `sentiment`.
- Use interactive labeling to correct sentiment and urgency simultaneously.
