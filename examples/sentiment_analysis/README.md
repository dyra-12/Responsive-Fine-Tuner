# Sentiment Analysis Example

Use this dataset to explore binary sentiment classification. Replace `sample.csv` with your own reviews when you are ready.

- `sample.csv`: columns `text`, `label` (`Positive`/`Negative`).
- Run `rft demo` and upload this CSV via the Data Upload tab.
